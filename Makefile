#enter to cabinet-exline-app container
api:
	docker exec -it dva-pacana-api bash

#enter to cabinet-exline-db container
db:
	docker exec -it dva-pacana-db bash

#enter to cabinet-exline-redis container
redis:
	docker exec -it dva-pacana-redis bash

#enter to cabinet-exline-nginx container
nginx:
	docker exec -it dva-pacana-nginx bash

